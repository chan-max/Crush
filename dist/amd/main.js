define(function () { 'use strict';

    var NODE_TYPE;
    (function (NODE_TYPE) {
        NODE_TYPE[NODE_TYPE["ELEMENT"] = 1] = "ELEMENT";
    })(NODE_TYPE || (NODE_TYPE = {}));

    /*-------------------------------------------------------*/
    document.querySelector('#style');

});
